CREATE TABLE IF NOT EXISTS soldadores (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    soldador_name VARCHAR(50) NOT NULL,
    password VARCHAR(255) NOT NULL,
    papel VARCHAR(30) NOT NULL
    );